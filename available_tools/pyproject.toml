[project]
name = "canvas-mcp-server"
version = "0.1.0"
description = "MCP server for Canvas LMS course authoring (assignments, quizzes, pages, modules)"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
